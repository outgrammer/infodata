# 信息源门户项目配置说明

## 技术栈

### 前端
- Vue 3 Composition API
- Vite
- Vue Router 4
- Element Plus (按需引入)
- Axios

### 后端
- Node.js
- Express.js
- SQLite
- Sequelize ORM

## 核心配置

### 环境变量配置

#### .env.development
开发环境配置文件，用于配置开发环境的API基础URL：
```
# 开发环境配置
VITE_API_BASE_URL=/api
```

#### .env.production
生产环境配置文件，用于配置生产环境的API基础URL：
```
# 生产环境配置
VITE_API_BASE_URL=/api
```

### 前端API服务层

#### Axios请求封装 (src/utils/request.js)
实现了完整的Axios请求封装，包括：
- 环境变量配置API基础URL
- 请求和响应拦截器
- 加载状态管理
- 统一错误处理
- 支持配置是否显示加载状态

#### API统一管理 (src/api/)
- sourceApi.js：信息源相关API接口
- qwenApi.js：AI辅助生成信息源接口
- index.js：API索引文件，统一管理所有API模块

### 后端中间件

#### 错误处理中间件 (app/middleware/errorHandler.js)
实现了完整的错误处理机制，包括：
- Sequelize验证错误处理
- Sequelize唯一约束错误处理
- 404错误处理
- 通用错误处理
- 开发环境错误详情显示

#### 数据验证中间件 (app/middleware/validate.js)
提供了强大的数据验证功能：
- 支持必填字段验证
- 类型验证
- 长度限制
- 自定义验证函数
- 数组验证
- URL验证
- 详细的错误信息

### 前端核心文件

#### vite.config.js
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({ resolvers: [ElementPlusResolver()] }),
    Components({ resolvers: [ElementPlusResolver()] })
  ],
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') }
  }
})
```

#### 前端入口 (src/main.js)
```javascript
import { createApp } from 'vue'
import './style.css'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import App from './App.vue'
import router from './router'

const app = createApp(App)
app.use(router)
app.use(ElementPlus)
app.mount('#app')
```

#### 路由配置 (src/router/index.js)
```javascript
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
      path: '/',
      name: 'home',
      component: () => import('@/views/HomeView.vue')
    }
  ]
})

export default router
```

### 后端核心文件

#### 数据库配置 (config/database.js)
```javascript
import { Sequelize } from 'sequelize'
import path from 'path'
import { fileURLToPath } from 'url'

const __filename = fileURLToPath(import.meta.url)
const __dirname = path.dirname(__filename)

const sequelize = new Sequelize({
  dialect: 'sqlite',
  storage: path.join(__dirname, '../data/infodata.db'),
  logging: console.log,
  define: {
    timestamps: true,
    underscored: true,
    freezeTableName: true
  }
})

export { sequelize }
```

#### 信息源模型 (app/models/sourceModel.js)
```javascript
import { DataTypes } from 'sequelize'
import { sequelize } from '../../config/database.js'

const Source = sequelize.define('source', {
  id: {
    type: DataTypes.INTEGER,
    primaryKey: true,
    autoIncrement: true
  },
  name: {
    type: DataTypes.STRING,
    allowNull: false,
    validate: { notEmpty: true, len: [1, 255] }
  },
  url: {
    type: DataTypes.STRING,
    allowNull: false,
    validate: { isUrl: true, len: [1, 2048] }
  },
  description: { type: DataTypes.TEXT, allowNull: true },
  category: { type: DataTypes.STRING, allowNull: true, validate: { len: [0, 100] } },
  tags: { type: DataTypes.JSON, allowNull: true, defaultValue: [] },
  icon: { type: DataTypes.STRING, allowNull: true, validate: { isUrl: true, len: [0, 2048] } }
}, {
  tableName: 'sources'
})

export default Source
```

#### 信息源控制器 (app/controllers/sourceController.js)
```javascript
import Source from '../models/sourceModel.js'

// 获取所有信息源
export const getAllSources = async (req, res) => {
  try {
    const sources = await Source.findAll()
    res.json({ success: true, data: sources })
  } catch (error) {
    res.status(500).json({ success: false, message: '获取信息源失败', error: error.message })
  }
}

// 获取单个信息源
export const getSourceById = async (req, res) => {
  try {
    const source = await Source.findByPk(req.params.id)
    if (!source) return res.status(404).json({ success: false, message: '信息源未找到' })
    res.json({ success: true, data: source })
  } catch (error) {
    res.status(500).json({ success: false, message: '获取信息源失败', error: error.message })
  }
}

// 创建、更新、删除方法省略...
```

#### 信息源路由 (app/routes/source.js)
```javascript
import express from 'express'
import { 
  getAllSources, 
  getSourceById, 
  createSource, 
  updateSource, 
  deleteSource 
} from '../controllers/sourceController.js'

const router = express.Router()

router.get('/sources', getAllSources)
router.get('/sources/:id', getSourceById)
router.post('/sources', createSource)
router.put('/sources/:id', updateSource)
router.delete('/sources/:id', deleteSource)

export default router
```

## 项目结构

```
infodata/
├── app/                  # 后端代码
│   ├── controllers/      # 控制器层
│   ├── models/           # 数据模型层
│   └── routes/           # 路由层
├── config/               # 配置文件
├── data/                 # SQLite数据库文件
├── src/                  # 前端代码
│   ├── api/              # API请求
│   ├── components/       # 公共组件
│   │   ├── SourceList.vue # 信息源列表组件
│   │   └── SourceForm.vue  # 信息源表单组件
│   ├── router/           # 路由配置
│   ├── utils/            # 工具函数
│   └── views/            # 页面组件
├── app.js                # Express应用配置
├── server.js             # 后端入口文件
└── vite.config.js        # Vite配置
```

## API接口

### 信息源API

#### GET /api/sources
获取信息源列表，支持分页、搜索和分类过滤。

**查询参数：**
| 参数名 | 类型 | 描述 | 默认值 |
|--------|------|------|--------|
| page | number | 页码 | 1 |
| pageSize | number | 每页数量 | 10 |
| search | string | 搜索关键词（按名称、描述或标签） | "" |
| category | string/array | 分类筛选（支持单个分类或分类数组） | "" |

**返回示例：**
```json
{
  "success": true,
  "data": {
    "sources": [
      {
        "id": 1,
        "name": "示例信息源",
        "url": "https://example.com",
        "description": "这是一个示例信息源",
        "category": "示例分类",
        "tags": ["示例", "测试"],
        "icon": "https://example.com/icon.png",
        "createdAt": "2025-12-17T12:32:10.939Z",
        "updatedAt": "2025-12-17T12:32:10.939Z"
      }
    ],
    "pagination": {
      "current": 1,
      "pageSize": 10,
      "total": 1,
      "totalPages": 1
    }
  }
}
```

#### GET /api/sources/:id
获取单个信息源详情。

**返回示例：**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "示例信息源",
    "url": "https://example.com",
    "description": "这是一个示例信息源",
    "category": "示例分类",
    "tags": ["示例", "测试"],
    "icon": "https://example.com/icon.png",
    "createdAt": "2025-12-17T12:32:10.939Z",
    "updatedAt": "2025-12-17T12:32:10.939Z"
  }
}
```

#### POST /api/sources
创建新信息源。

**请求体：**
```json
{
  "name": "新信息源",
  "url": "https://newexample.com",
  "description": "这是一个新的信息源",
  "category": "新分类",
  "tags": ["新", "测试"],
  "icon": "https://newexample.com/icon.png"
}
```

**返回示例：**
```json
{
  "success": true,
  "data": {
    "id": 2,
    "name": "新信息源",
    "url": "https://newexample.com",
    "description": "这是一个新的信息源",
    "category": "新分类",
    "tags": ["新", "测试"],
    "icon": "https://newexample.com/icon.png",
    "createdAt": "2025-12-17T13:00:00.000Z",
    "updatedAt": "2025-12-17T13:00:00.000Z"
  },
  "message": "信息源创建成功"
}
```

#### PUT /api/sources/:id
更新信息源。

**请求体：**
```json
{
  "name": "更新后的信息源",
  "description": "更新后的描述信息"
}
```

**返回示例：**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "更新后的信息源",
    "url": "https://example.com",
    "description": "更新后的描述信息",
    "category": "示例分类",
    "tags": ["示例", "测试"],
    "icon": "https://example.com/icon.png",
    "createdAt": "2025-12-17T12:32:10.939Z",
    "updatedAt": "2025-12-17T13:30:00.000Z"
  },
  "message": "信息源更新成功"
}
```

#### DELETE /api/sources/:id
删除信息源。

**返回示例：**
```json
{
  "success": true,
  "message": "信息源删除成功"
}
```

#### GET /health
健康检查。

**返回示例：**
```json
{
  "status": "ok",
  "message": "Server is running"
}
```

#### POST /api/ai/generate
AI辅助生成信息源详细信息。

**请求体：**
```json
{
  "input": "GitHub"
}
```

**返回示例：**
```json
{
  "success": true,
  "data": {
    "name": "GitHub",
    "url": "https://github.com",
    "description": "GitHub是一个面向开源及私有软件项目的托管平台，因为只支持Git作为唯一的版本库格式进行托管，故名GitHub。",
    "category": "代码托管",
    "tags": ["代码托管", "开源", "Git", "开发者工具"]
  },
  "message": "信息生成成功"
}
```

## 使用说明

### 安装依赖
```bash
npm install
```

### 启动服务

#### 前端开发服务器
```bash
npm run dev
```

#### 后端开发服务器
```bash
npm run server:dev
```

### 构建生产版本
```bash
npm run build
```

## 核心功能

### 前端
- 信息源列表展示
- 高级搜索过滤（按名称、描述和标签多字段匹配）
- 多分类筛选（支持同时选择多个分类）
- 搜索结果高亮显示
- 响应式设计
- 点击卡片打开信息源链接
- 添加信息源功能
- 编辑信息源功能
- AI辅助添加信息源（自动生成标签、分类、描述等）
- 完整的API服务层
- 环境变量配置
- 加载状态管理
- 统一错误处理

### 后端
- 完整的RESTful API设计
- 信息源CRUD操作
- SQLite数据库存储
- Sequelize ORM简化数据库操作
- 自动数据库初始化
- 分页、搜索和分类过滤支持
- 强大的数据验证机制
- 完整的错误处理
- 支持RESTful API最佳实践

### 中间件
- 数据验证中间件：确保请求数据合法
- 错误处理中间件：统一处理各类错误
- 404处理中间件：友好的资源未找到提示

### API特性
- 支持分页、搜索和分类过滤
- 详细的API文档
- 统一的响应格式
- 开发环境错误详情
- 生产环境安全提示

### 前端API服务层
- Axios请求封装
- 环境变量配置API基础URL
- 请求和响应拦截器
- 加载状态管理
- 统一错误处理
- API统一管理
- 支持配置是否显示加载状态
- 完整的TypeScript类型定义

## 关键组件

### SourceList.vue
核心信息源列表组件，实现了：
- 卡片式布局展示信息源
- 高级搜索过滤（按名称、描述和标签多字段匹配）
- 多分类筛选（支持同时选择多个分类）
- 搜索结果高亮显示
- 实时搜索和筛选
- 搜索结果数量统计
- 响应式设计
- 空状态处理
- 添加信息源功能
- 编辑信息源功能
- 提取所有标签用于自动补全

### SourceForm.vue
信息源管理表单组件，实现了：
- 完整的表单验证
- 支持添加和编辑模式
- 动态分类选择（支持新建分类）
- 标签输入和自动补全
- 响应式设计
- 表单提交到后端API

## 数据库表结构

### sources表
| 字段名 | 类型 | 约束 | 描述 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键，自增 |
| name | VARCHAR(255) | NOT NULL | 信息源名称 |
| url | VARCHAR(255) | NOT NULL | 信息源链接 |
| description | TEXT | NULL | 描述信息 |
| category | VARCHAR(100) | NULL | 分类标签 |
| tags | JSON | NULL | 标签数组，默认[] |
| icon | VARCHAR(255) | NULL | 图标链接 |
| created_at | DATETIME | NOT NULL | 创建时间 |
| updated_at | DATETIME | NOT NULL | 更新时间 |
